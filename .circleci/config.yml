version: 2.1

orbs:
  node: circleci/node@4.7

jobs:
  build:
    docker:
      - image: cimg/node:14.19.0
    steps:
      - checkout
      - run:  cd interface && npm install 
      - run: echo "buildando aplicação"
      - run: cd interface && npm start

  test:
    docker:
      - image: cimg/node:14.19.0
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run: echo "Testando 1... 2... 3..."
      
workflows:
  sample:
    jobs:
      - build
      - test